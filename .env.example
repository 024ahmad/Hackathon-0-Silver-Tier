# AI Employee — Environment Variable Reference
# Copy this file to .env and adjust values as needed.
# .env is gitignored and must NEVER be committed.

# Polling interval for the filesystem watcher (milliseconds)
WATCH_INTERVAL_MS=250

# Dry-run mode: if true, watcher logs what it WOULD do without writing any files.
# Set to false only in a live environment after verifying watcher behavior.
DRY_RUN=true
